const formProfile = document.forms.formPopup;
const formAddCard = document.forms.formAddCard;

const inputName = formProfile.elements.inputName;
const inputMore= formProfile.elements.inputMore;

const buttonEditProfile = document.querySelector('.profile__edit-button');
const buttonOpenPopupCard = document.querySelector('.profile__add-button');
export {formProfile, formAddCard, inputName, inputMore, buttonEditProfile, buttonOpenPopupCard}